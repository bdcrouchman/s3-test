module.exports = function(index) {
	let startId = 0;
	let numReads = 1;
	console.log(`Creating JSON: startingId ${startId}, numReads ${numReads}`)
	return {
		startingId: 0,
		numReads: numReads
	};
}
